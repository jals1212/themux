# vim:set ft=tmux:
%hidden MODULE_NAME="application"

set -ogq "@themux_${MODULE_NAME}_icon" ""
set -ogq "@themux_${MODULE_NAME}_color" "#{E:@thm_maroon}"
set -ogq "@themux_${MODULE_NAME}_text" "#{pane_current_command}"

source -F "#{d:current_file}/../utils/module_block.conf"
