# vim:set ft=tmux:
%hidden MODULE_NAME="weather"

# Requires https://github.com/xamut/tmux-weather.

set -ogq @themux_weather_icon " "
set -ogqF @themux_weather_color "#{E:@thm_yellow}"
set -ogq @themux_weather_text " #{l:#{weather}}"

source -F "#{d:current_file}/../utils/status_module.conf"
