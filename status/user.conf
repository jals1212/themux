# vim:set ft=tmux:
%hidden MODULE_NAME="user"

set -ogq @themux_user_icon " "
set -ogqF @themux_user_color "#{E:@thm_sky}"
set -ogq @themux_user_text " #(whoami)"

source -F "#{d:current_file}/../utils/status_module.conf"
