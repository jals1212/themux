# vim:set ft=tmux:
%hidden MODULE_NAME="user"

set -ogq @themux_${MODULE_NAME}_icon " "
set -ogqF @themux_${MODULE_NAME}_color "#{E:@thm_sky}"
set -ogq @themux_${MODULE_NAME}_text " #(whoami)"

source -F "#{d:current_file}/../utils/status_module.conf"
