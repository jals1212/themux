# vim:set ft=tmux:

# Rounded caps for the optional pane status; the label comes from panes_block.
set -gq @themux_pane_left_border ""
set -gq @themux_pane_right_border ""

source -F "#{d:current_file}/../../utils/panes_block.conf"
