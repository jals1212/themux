# vim:set ft=tmux:

# Shared block renderer for the squared/rounded/slanted window variants.
# Expects @themux_window_*_separator to be set by the variant file.
set -gF window-status-activity-style "bg=#{@thm_lavender},fg=#{@thm_crust}"
set -gF window-status-bell-style "bg=#{@thm_yellow},fg=#{@thm_crust}"

source -F "#{d:current_file}/window_flags.conf"

# DO NOT USE -o IN YOUR OWN CONFIGURATION
set -ogqF @themux_window_current_left_border "#{@themux_window_left_border}"
set -ogqF @themux_window_current_middle_separator "#{@themux_window_middle_separator}"
set -ogqF @themux_window_current_right_border "#{@themux_window_right_border}"

set -g @_tmx_w_number_style "#[fg=#{@thm_crust},bg=#{@themux_window_number_color}]"
set -g @_tmx_w_text_style "#[fg=#{@thm_fg},bg=#{@themux_window_text_color}]"
%if "#{==:#{@themux_window_number_position},left}"
  set -gF window-status-format \
    "#{E:@_tmx_w_number_style}#{E:@themux_window_left_border}#{@themux_window_number}"
  set -agF window-status-format "#{E:@themux_window_middle_separator}"
  set -agF window-status-format \
    "#{E:@_tmx_w_text_style}#{@themux_window_text}#{@_tmx_w_flags}#{E:@themux_window_right_border}"
%else
  set -gF window-status-format \
    "#{E:@_tmx_w_text_style}#{E:@themux_window_left_border}#{E:@_tmx_w_text_style}#{@themux_window_text}#{@_tmx_w_flags}"
  set -agF window-status-format "#{E:@themux_window_middle_separator}"
  set -agF window-status-format \
    "#{E:@_tmx_w_number_style} #{@themux_window_number}#{E:@themux_window_right_border}"
%endif

# =======================================
# And do the same for the current window.
# =======================================

set -g @_tmx_w_number_style "#[fg=#{@thm_crust},bg=#{@themux_window_current_number_color}]"
set -g @_tmx_w_text_style "#[fg=#{@thm_fg},bg=#{@themux_window_current_text_color}]"
%if "#{==:#{@themux_window_number_position},left}"
  set -gF window-status-current-format \
    "#{E:@_tmx_w_number_style}#{E:@themux_window_current_left_border}#{@themux_window_current_number}"
  set -agF window-status-current-format "#{E:@themux_window_current_middle_separator}"
  set -agF window-status-current-format \
    "#{E:@_tmx_w_text_style}#{@themux_window_current_text}#{@_tmx_w_flags}#{E:@themux_window_current_right_border}"
%else
  set -gF window-status-current-format \
    "#{E:@_tmx_w_text_style}#{E:@themux_window_current_left_border}#{E:@_tmx_w_text_style}#{@themux_window_current_text}#{@_tmx_w_flags}"
  set -agF window-status-current-format "#{E:@themux_window_current_middle_separator}"
  set -agF window-status-current-format \
    "#{E:@_tmx_w_number_style} #{@themux_window_current_number}#{E:@themux_window_current_right_border}"
%endif

# Cleanup (unset) all of the internal variables.
set -ug @_tmx_w_number_style
set -ug @_tmx_w_text_style
set -ug @_tmx_w_flags
